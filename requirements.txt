openai==1.51.0
# Pin httpx below 1.0 because openai 1.51.0 passes `proxies=` to httpx.Client
httpx==0.27.2
# (httpx 0.27 works with anyio<5; the resolver will pick a compatible version automatically)
